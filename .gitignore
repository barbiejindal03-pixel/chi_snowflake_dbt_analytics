# dbt build artifacts (regenerated by `dbt run`/`dbt docs generate` — no need to commit)
target/
dbt_packages/
logs/
.user.yml

# local dev database (DuckDB test run)
dev.duckdb
*.duckdb

# dbt connection profile — NEVER commit real Snowflake credentials
profiles.yml
!profiles_example.yml

# original source spreadsheet — contains real client PII, must never be committed
*.xlsx
*.xls
CLIENT_MASTER*
VISIT_LOG*

# OS / editor junk
.DS_Store
.vscode/
__pycache__/
*.pyc
